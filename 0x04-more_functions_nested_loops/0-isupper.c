#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * Return: 1 for upper case or 0 for the latter
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}


